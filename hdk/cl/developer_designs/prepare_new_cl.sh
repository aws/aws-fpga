#!/bin/bash

# TODO
# Check if /build and /design directories exist, abort
# Check if $HDK_COMMON_DIR exist

cp -r $HDK_COMMON_DIR/new_cl_template/build .
cp -r $HDK_COMMON_DIR/new_cl_template/design .
